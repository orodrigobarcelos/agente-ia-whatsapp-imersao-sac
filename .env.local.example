# Arquivo gerado pelo Claude Code durante a instalação.
# Você pode editar manualmente se mudar a Railway / regenerar credenciais.

# URL pública do Postgres (Railway → Postgres → Variables → DATABASE_PUBLIC_URL)
DATABASE_URL=

# URL pública do agente (Railway → Agente → Settings → Networking → Public Domain)
AGENT_URL=
